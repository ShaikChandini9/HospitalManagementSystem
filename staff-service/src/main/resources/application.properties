spring.application.name=staff-service

# ===============================
# Server Configuration
# ===============================
server.port=8085

# ===============================
# Database Configuration (MySQL)
# ===============================
spring.datasource.url=jdbc:mysql://localhost:3306/hospitalpatientdetails?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Chandini@5d2
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ===============================
# Hibernate Configuration
# ===============================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
logging.level.org.springframework.security=DEBUG

#For docker
spring.docker.compose.enabled=false

#For dynatrace
#management.dynatrace.metrics.export.enabled=false


# Explicit MySQL Dialect
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# ===============================
# H2 Console (For Testing)
# ===============================
spring.h2.console.enabled=false

# ===============================
# Connection Pooling (Optional)
# ===============================
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.connection-timeout=30000

#jwt.secret=6rRdsdjfklwejrlwreowqriuwelkrwjfhrwueofkldjfoweiru...
#jwt.expiration=86400000

sspring.session.store-type=jdbc
spring.session.jdbc.initialize-schema=always
spring.datasource.initialize=true
pring.session.store-type=jdbc
spring.session.jdbc.cleanup-cron=-
